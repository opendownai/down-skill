{
  "name": "opendown",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "update-data": "node update-data.mjs",
    "deploy": "wrangler deploy dist/worker.js --name opendown"
  }
}
